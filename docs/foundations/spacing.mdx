import { Meta } from "@storybook/blocks";

<Meta title="Foundations / Spacing" />

# Spacing

Lo spacing in SoliDS è basato su una **scala coerente e limitata**.

L’obiettivo è:

- ridurre variazioni arbitrarie
- migliorare l’allineamento
- rendere il layout prevedibile e scalabile

---

## Regole fondamentali

- ✅ Usa **solo** valori della scala
- ❌ Non usare `px` arbitrari nei componenti
- ✅ Preferisci token semantici allo spacing “ad hoc”

---

## Spacing scale

| Token          | Uso consigliato      |
| -------------- | -------------------- |
| `--sd-space-0` | reset / collapse     |
| `--sd-space-1` | gap minimo           |
| `--sd-space-2` | padding interno      |
| `--sd-space-3` | separazione elementi |
| `--sd-space-4` | layout standard      |
| `--sd-space-5` | layout ampio         |
| `--sd-space-6` | separazioni macro    |

---

## Visual preview

### Light theme

<div
  data-theme="light"
  style={{
    padding: 16,
    background: "var(--sd-color-bg-canvas)",
    borderRadius: 12,
  }}
>
  <SpacingScale />
</div>

### Dark theme

<div
  data-theme="dark"
  style={{
    padding: 16,
    background: "var(--sd-color-bg-canvas)",
    borderRadius: 12,
  }}
>
  <SpacingScale />
</div>

---

export function SpacingScale() {
  const items = [
    { name: "space-0", var: "--sd-space-0" },
    { name: "space-1", var: "--sd-space-1" },
    { name: "space-2", var: "--sd-space-2" },
    { name: "space-3", var: "--sd-space-3" },
    { name: "space-4", var: "--sd-space-4" },
    { name: "space-5", var: "--sd-space-5" },
    { name: "space-6", var: "--sd-space-6" }
  ];

return (
<div style={{ display: "flex", flexDirection: "column", gap: 12 }}>
{items.map((item) => (
<div
key={item.var}
style={{
            display: "flex",
            alignItems: "center",
            gap: 12,
            color: "var(--sd-color-text-primary)"
          }} >
<div style={{ width: 120, fontSize: 12 }}>
{item.name}
<div style={{ opacity: 0.6 }}>{item.var}</div>
</div>

          <div
            style={{
              height: 16,
              width: `var(${item.var})`,
              minWidth: 1,
              background: "var(--sd-color-intent-info)",
              borderRadius: 4
            }}
          />

          <div style={{ fontSize: 12, opacity: 0.6 }}>
            var({item.var})
          </div>
        </div>
      ))}
    </div>

);
}
